{
  "name": "@meetergo/mcp-server",
  "version": "0.2.0",
  "description": "meetergo MCP server — let AI agents book, reschedule and cancel meetings",
  "license": "MIT",
  "type": "module",
  "bin": {
    "meetergo-mcp": "dist/index.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "scheduling",
    "calendar",
    "meetergo"
  ],
  "homepage": "https://meetergo.com/mcp",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/meetergo/meetergo-mcp-server.git"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.2",
    "zod": "^3.25.76"
  },
  "bugs": {
    "url": "https://github.com/meetergo/meetergo-mcp-server/issues"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.1.10",
    "@types/node": "^22.18.0"
  }
}
